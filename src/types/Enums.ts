export enum Method {
    CASH = 'CASH',
    CARD = 'CARD',
}

export enum Position {
    WAITER = 'WAITER',
    COOK = 'COOK',
    BARTENDER = 'BARTENDER',
    MANAGER = 'MANAGER',
}

export enum Role {
    SUPER_ADMIN = 'SUPER_ADMIN',
    ADMIN = 'ADMIN',
    STAFF = 'STAFF',
    CUSTOMER = 'CUSTOMER',
}

export enum Status {
    PENDING = 'PENDING',
    COMPLETED = 'COMPLETED',
    CANCELLED = 'CANCELLED',
    IN_PROGRESS = 'IN_PROGRESS',
}

export enum Type {
    PAYMENT = 'PAYMENT',
    REFUND = 'REFUND',
}
